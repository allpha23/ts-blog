export type TypeBlogs = {
  userId: number;
  id: number;
  title: string;
  body: string;
};

export type TypeUsers = {
  id: number;
  username: string;
};

export type TypeComments = {
  id: number;
  name: string;
  body: string;
};
